<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout  xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@color/white"
    android:layout_height="match_parent"
    tools:context="com.kj.app.view.activity.MusicActivity">

    <include layout="@layout/custom_bar" android:id="@+id/bar"></include>

    <include
        android:id="@+id/null_data"
        layout="@layout/custom_null_data"
        android:layout_below="@id/bar"
        android:visibility="gone"
        android:layout_height="match_parent"
        android:layout_width="match_parent"></include>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_music"
        android:layout_below="@id/bar"
        android:paddingLeft="8dp"
        android:paddingTop="8dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </android.support.v7.widget.RecyclerView>

    <android.support.v7.widget.CardView
        android:layout_width="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        app:cardCornerRadius="20dp"
        android:layout_marginRight="35dp"
        android:layout_marginBottom="25dp"
        app:cardElevation="@dimen/dp_10"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/iv_music_dw"
            android:src="@mipmap/dw"
            android:padding="6dp"
            android:visibility="gone"
            android:layout_width="36dp"
            android:layout_height="36dp" />

    </android.support.v7.widget.CardView>

</RelativeLayout>
